<IfModule mod_rewrite.c>
    RewriteEngine On

    # Ensure admin subdomain lands on /admin
    RewriteRule ^$ /admin [L,R=302]

    # SPA fallback for admin site
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule . /index.html [L]
</IfModule>
